# HeroesApi - Управление героями с JSON

**ФИО:** Ткаченко ВМ
**Группа:** ИСП-231

## Таблица всех маршрутов

| Метод | URL | Описание | Коды ответа |
| --- | --- | --- | --- |
| GET | /api/heroes | Получить всех героев | 200 |
| GET | /api/heroes/{id} | Получить героя по ID | 200, 404 |
| GET | /api/heroes/demo | Сравнение настроек сериализации (по умолчанию vs настроенный) | 200 |
| GET | /api/heroes/serialize | Демонстрация работы JsonSerializer (сериализация и десериализация) | 200 |

## Таблица атрибутов JSON

| Атрибут / настройка | Что делает | Пример |
| --- | --- | --- |
| [JsonPropertyName("name")] | Задаёт имя поля в JSON | "name" вместо "Name" |
| [JsonIgnore] | Поле не попадает в JSON | Пароли, внутренние поля |
| PropertyNamingPolicy.CamelCase | Все поля в camelCase | "realName" вместо "RealName" |
| JsonStringEnumConverter | Enum как строка | "Marvel" вместо 0 |
| WriteIndented = true | JSON с отступами | Для удобства при разработке |

## Главные выводы

1. **Что такое сериализация и десериализация** — сериализация это преобразование C#-объекта в JSON-строку для отправки клиенту, десериализация — обратное преобразование JSON-строки в C#-объект при получении данных от клиента.

2. **Зачем нужен [JsonIgnore]** — этот атрибут исключает поле из JSON-ответа. Реальный пример: поле `InternalNotes` (внутренние заметки разработчика) или пароли, которые не должны попадать в ответ API.

3. **Почему enum лучше сериализовывать строкой а не числом** — строка "Marvel" понятна человеку и клиентскому коду, а число 0 требует документации и дополнительных преобразований. Строковое представление самодокументируемо.

4. **Что изменилось в ответе после настройки CamelCase** — все имена полей стали с маленькой буквы: `realName` вместо `RealName`, `powerLevel` вместо `PowerLevel`. Это стандарт JSON-API, ожидаемый фронтендом.

## Итоговая таблица: что изучили в лабораторной

| Концепция | Описание |
| --- | --- |
| JSON | Текстовый формат для передачи данных между сервером и клиентом |
| Сериализация | Объект C# → JSON-строка |
| Десериализация | JSON-строка → объект C# |
| [JsonPropertyName("name")] | Задать точное имя поля в JSON |
| [JsonIgnore] | Исключить поле из JSON |
| JsonNamingPolicy.CamelCase | Все поля автоматически в camelCase |
| JsonStringEnumConverter | Enum сериализуется как строка, не число |
| WriteIndented = true | JSON с отступами — читаемый вид |
| List&lt;string&gt; | В JSON становится массивом ["a","b","c"] |
| Вложенный объект | Класс внутри класса → объект внутри объекта в JSON |
| JsonSerializer.Serialize() | Ручная сериализация объекта в строку |
| JsonSerializer.Deserialize&lt;T&gt;() | Ручная десериализация строки в объект |
